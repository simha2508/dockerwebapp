---
version: "3"
services:
 devopsdb:
   container_name: devopsdb
   build: ./Docker-db
   ports:
     - "3306:3306"

 application:
   container_name: containerapp
   build: ./Docker-app
   ports:
     - "1234:8080"
   depends_on:
     - "devopsdb"
